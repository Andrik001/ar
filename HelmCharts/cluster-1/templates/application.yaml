apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: grafana-cluster-1
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'git@github.com:Andrik001/ar.git'
    path: HelmCharts/{{ .Values.cluster1.helm.application }}
    targetRevision: HEAD
    helm:
      valueFiles: 
        - values-cluster-2.yaml
        # - {{ .Values.cluster1.helm.valueFiles }}    
  destination:
    name: cluster-1
    namespace: grafana
  syncPolicy:
    automated: {}
    syncOptions:
      - CreateNamespace=true